{
  "name": "themeisle-tester",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "scripts": "wp-build",
    "dev": "wp-build --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "biome format --write",
    "lint": "biome lint",
    "lint:fix": "biome lint --write",
    "check": "biome check --write"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@wordpress/build": "^0.14.0"
  }
}
